---
phase: 02-core-crypto-and-auth
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/sdk/src/errors/index.ts
  - packages/sdk/src/auth/keypair.ts
  - packages/sdk/tests/errors.test.ts
  - packages/sdk/tests/keypair.test.ts
autonomous: true
requirements:
  - TYPE-03
  - AUTH-01
  - AUTH-06

must_haves:
  truths:
    - "HeyLolError, AuthError, PaymentRejectedError, NetworkError are all importable from errors/index.ts"
    - "Each error class has a string literal 'code' discriminant property and extends Error"
    - "instanceof checks work correctly for all error subclasses"
    - "loadKeypair() accepts a 64-byte base58 Solana keypair string and returns { secretKey, publicKey } as Uint8Array"
    - "loadKeypair() accepts a 32-byte base58 secret key and derives the public key via ed25519.getPublicKey"
    - "loadKeypair() throws AuthError with code INVALID_PRIVATE_KEY for invalid input lengths"
    - "All crypto uses @noble/curves and @scure/base — zero Node.js built-ins"
  artifacts:
    - path: "packages/sdk/src/errors/index.ts"
      provides: "Discriminated union error hierarchy"
      exports: ["HeyLolError", "AuthError", "PaymentRejectedError", "NetworkError", "SdkError", "isSdkError"]
    - path: "packages/sdk/src/auth/keypair.ts"
      provides: "Base58 keypair loading with pure-JS crypto"
      exports: ["Keypair", "loadKeypair"]
    - path: "packages/sdk/tests/errors.test.ts"
      provides: "Error hierarchy tests"
    - path: "packages/sdk/tests/keypair.test.ts"
      provides: "Keypair loading tests with byte-level assertions"
  key_links:
    - from: "packages/sdk/src/auth/keypair.ts"
      to: "packages/sdk/src/errors/index.ts"
      via: "import AuthError for invalid key errors"
      pattern: "import.*AuthError.*from.*errors"
    - from: "packages/sdk/src/auth/keypair.ts"
      to: "@noble/curves/ed25519.js"
      via: "ed25519.getPublicKey for 32-byte key derivation"
      pattern: "ed25519\\.getPublicKey"
    - from: "packages/sdk/src/auth/keypair.ts"
      to: "@scure/base"
      via: "base58.decode for key input"
      pattern: "base58\\.decode"
---

<objective>
Establish the error hierarchy and keypair loading that every subsequent plan in Phase 2 depends on.

Purpose: Error types define the contract for all SDK errors (TYPE-03). Keypair loading is the entry point for auth (AUTH-01). Both must exist before Solana serialization or x402 parsing can be built.

Output: `errors/index.ts` with 4 error classes + type guard, `auth/keypair.ts` with `loadKeypair()`, and passing TDD tests for both.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-crypto-and-auth/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error hierarchy with discriminated union types</name>
  <files>packages/sdk/src/errors/index.ts, packages/sdk/tests/errors.test.ts</files>
  <action>
RED: Create `packages/sdk/tests/errors.test.ts` with tests:
- `new HeyLolError({ code: 'TEST', message: 'msg' })` produces error with `.code === 'TEST'`, `.message === 'msg'`, `.name === 'HeyLolError'`
- `new AuthError({ code: 'INVALID_PRIVATE_KEY', message: 'bad key' })` — `instanceof AuthError === true`, `instanceof HeyLolError === true`, `instanceof Error === true`
- `new PaymentRejectedError({ code: 'PAYMENT_REJECTED', message: 'rejected', reason: 'bad sig' })` — `.reason === 'bad sig'`, instanceof checks pass
- `new NetworkError({ code: 'TIMEOUT', message: 'timed out', statusCode: 408 })` — `.statusCode === 408`, instanceof checks pass
- `isSdkError(new AuthError(...))` returns `true`; `isSdkError(new Error('plain'))` returns `false`
- `.toJSON()` returns `{ name, code, message }` — no secret leakage
- TypeScript narrowing: verify `code` property is properly typed as string literal union (test that assigning wrong code literal is caught — this is a compile-time check, document in test comments)

Run tests — they MUST fail (no implementation yet).

GREEN: Create `packages/sdk/src/errors/index.ts`:
- `HeyLolError extends Error` with `readonly code: string`, constructor takes `{ code, message }`, sets `this.name = 'HeyLolError'`, includes `toJSON()` that returns `{ name, code, message }`
- `AuthError extends HeyLolError` with `code` typed as `'INVALID_PRIVATE_KEY' | 'KEY_DECODE_FAILED' | 'SIGNING_FAILED' | 'X402_PARSE_FAILED' | 'TRANSACTION_BUILD_FAILED'`, sets `this.name = 'AuthError'`
- `PaymentRejectedError extends HeyLolError` with `code` typed as `'PAYMENT_REJECTED' | 'INSUFFICIENT_FUNDS' | 'INVALID_SIGNATURE' | 'AMOUNT_MISMATCH'`, optional `readonly reason?: string`, sets `this.name = 'PaymentRejectedError'`
- `NetworkError extends HeyLolError` with `code` typed as `'FETCH_FAILED' | 'TIMEOUT' | 'RATE_LIMITED'`, optional `readonly statusCode?: number`, sets `this.name = 'NetworkError'`
- `type SdkError = AuthError | PaymentRejectedError | NetworkError`
- `function isSdkError(e: unknown): e is SdkError` — returns `e instanceof HeyLolError`

tsconfig target is ES2022 — `Object.setPrototypeOf` is NOT needed (native class extends works correctly in ES2022). Do NOT add it.

Run tests — they MUST pass.
  </action>
  <verify>
`cd /Users/rawgroundbeef/Projects/heylol && pnpm --filter @heylol/sdk test` — errors.test.ts passes.
  </verify>
  <done>All 4 error classes instantiate correctly, instanceof chains work, toJSON excludes secrets, isSdkError type guard works, code property is typed as string literal union.</done>
</task>

<task type="auto">
  <name>Task 2: Keypair loading from base58 with TDD</name>
  <files>packages/sdk/src/auth/keypair.ts, packages/sdk/tests/keypair.test.ts</files>
  <action>
First, install the runtime dependencies needed for this task. Run from the monorepo root:
```
pnpm add @noble/curves @scure/base --filter @heylol/sdk
```

RED: Create `packages/sdk/tests/keypair.test.ts` with tests:
- Generate a known test keypair: use `ed25519.keygen()` (from `@noble/curves/ed25519.js`) with a fixed seed or use a hardcoded base58 keypair string. The test needs a deterministic keypair.
- Test 64-byte input: encode `concat(secretKey, publicKey)` as base58, call `loadKeypair(encoded)` → verify `result.secretKey` equals original 32-byte secret, `result.publicKey` equals original 32-byte public
- Test 32-byte input: encode just `secretKey` as base58, call `loadKeypair(encoded)` → verify `result.publicKey` equals `ed25519.getPublicKey(secretKey)`
- Test invalid length (e.g., 16 bytes base58-encoded): `loadKeypair(encoded)` throws `AuthError` with `code === 'INVALID_PRIVATE_KEY'`
- Test invalid base58 string (contains '0', 'O', 'I', 'l'): throws (either AuthError or base58 decode error is acceptable)
- Test returned Keypair has `readonly` properties (TypeScript compile check — document in comments)

For deterministic test data, use `@scure/base`'s `base58.encode()` on known byte arrays. Example fixture:
```typescript
const SECRET_KEY = new Uint8Array(32).fill(1); // known 32-byte key
const PUBLIC_KEY = ed25519.getPublicKey(SECRET_KEY);
const KEYPAIR_64 = new Uint8Array([...SECRET_KEY, ...PUBLIC_KEY]);
const BASE58_64 = base58.encode(KEYPAIR_64);
const BASE58_32 = base58.encode(SECRET_KEY);
```

Run tests — they MUST fail (no implementation yet).

GREEN: Create `packages/sdk/src/auth/keypair.ts`:
- `import { base58 } from '@scure/base';`
- `import { ed25519 } from '@noble/curves/ed25519.js';` (NOTE: `.js` extension is REQUIRED for @noble/curves v2)
- `import { AuthError } from '../errors/index.js';` (use `.js` extension for ESM)
- Export `interface Keypair { readonly secretKey: Uint8Array; readonly publicKey: Uint8Array; }`
- Export `function loadKeypair(privateKeyBase58: string): Keypair` that:
  1. Calls `base58.decode(privateKeyBase58)` to get `decoded: Uint8Array`
  2. If `decoded.length === 64`: split into `secretKey = decoded.slice(0, 32)`, `publicKey = decoded.slice(32, 64)`, return `{ secretKey, publicKey }`
  3. If `decoded.length === 32`: `secretKey = decoded`, `publicKey = ed25519.getPublicKey(secretKey)`, return `{ secretKey, publicKey }`
  4. Otherwise: `throw new AuthError({ code: 'INVALID_PRIVATE_KEY', message: 'Expected 32 or 64 byte base58 private key, got ${decoded.length} bytes' })`
  5. Wrap the entire function body in try/catch — if the error is already `AuthError` re-throw, otherwise throw `new AuthError({ code: 'KEY_DECODE_FAILED', message: 'Failed to decode base58 private key: ${String(error)}' })`

Run tests — they MUST pass.
  </action>
  <verify>
`cd /Users/rawgroundbeef/Projects/heylol && pnpm --filter @heylol/sdk test` — keypair.test.ts passes. Verify no Node.js built-in imports: `pnpm --filter @heylol/sdk lint` passes.
  </verify>
  <done>loadKeypair handles 32-byte and 64-byte base58 inputs, derives public key for 32-byte input, throws typed AuthError for invalid inputs, uses only @noble/curves and @scure/base (zero Node.js built-ins).</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @heylol/sdk test` — all tests in errors.test.ts and keypair.test.ts pass
2. `pnpm --filter @heylol/sdk lint` — no restricted Node.js imports detected
3. Error classes: `instanceof` chains work (AuthError instanceof HeyLolError instanceof Error)
4. Keypair: 64-byte and 32-byte base58 inputs both produce correct Keypair objects
5. All crypto from @noble/curves and @scure/base — zero Node.js built-ins
</verification>

<success_criteria>
- Error hierarchy has 4 classes with typed code discriminants and isSdkError type guard
- loadKeypair accepts 32-byte and 64-byte base58 keys and returns typed Keypair
- All tests pass with red-green TDD discipline
- Zero Node.js built-in imports in SDK source
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-crypto-and-auth/02-01-SUMMARY.md`
</output>
