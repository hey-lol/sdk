---
phase: 06-adapters-docs-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/adapter-cloudflare/src/index.ts
  - packages/adapter-cloudflare/package.json
  - packages/adapter-cloudflare/tsup.config.ts
  - packages/adapter-cloudflare/tests/cloudflare.test.ts
  - packages/adapter-vercel/src/index.ts
  - packages/adapter-vercel/package.json
  - packages/adapter-vercel/tsup.config.ts
  - packages/adapter-vercel/tests/vercel.test.ts
  - packages/adapter-express/src/index.ts
  - packages/adapter-express/package.json
  - packages/adapter-express/tsup.config.ts
  - packages/adapter-express/tests/express.test.ts
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03

must_haves:
  truths:
    - "CloudflareClient reads HEYLOL_PRIVATE_KEY from env binding and creates a working HeyLolClient"
    - "VercelClient reads HEYLOL_PRIVATE_KEY from process.env and creates a working HeyLolClient"
    - "createNextjsMiddleware returns a function compatible with Next.js middleware signature"
    - "createHeyLolMiddleware returns an Express RequestHandler that attaches heyLolClient to req"
    - "createFromEnv factory creates a HeyLolClient from Cloudflare env bindings"
    - "All three adapter packages build, typecheck, and pass attw"
  artifacts:
    - path: "packages/adapter-cloudflare/src/index.ts"
      provides: "CloudflareClient class + createFromEnv factory + HeyLolEnv interface"
      exports: ["CloudflareClient", "createFromEnv", "HeyLolEnv"]
    - path: "packages/adapter-vercel/src/index.ts"
      provides: "VercelClient class + createNextjsMiddleware helper"
      exports: ["VercelClient", "createNextjsMiddleware", "NextjsMiddlewareOptions"]
    - path: "packages/adapter-express/src/index.ts"
      provides: "createHeyLolMiddleware factory + Express Request augmentation"
      exports: ["createHeyLolMiddleware", "HeyLolMiddlewareOptions"]
  key_links:
    - from: "packages/adapter-cloudflare/src/index.ts"
      to: "@heylol/sdk"
      via: "import HeyLolClient, extends it"
      pattern: "extends HeyLolClient"
    - from: "packages/adapter-vercel/src/index.ts"
      to: "@heylol/sdk"
      via: "import HeyLolClient, extends it"
      pattern: "extends HeyLolClient"
    - from: "packages/adapter-express/src/index.ts"
      to: "@heylol/sdk"
      via: "import HeyLolClient, new instance in middleware"
      pattern: "new HeyLolClient"
---

<objective>
Implement all three runtime adapter packages: CloudflareClient for Workers env bindings, VercelClient for Vercel Edge with Next.js middleware helper, and Express middleware factory with request augmentation.

Purpose: Developers on each platform get idiomatic initialization patterns without needing to understand HeyLolClient constructor details. Each adapter is a thin wrapper (~50-80 lines) over the existing HeyLolClient.

Output: Three fully implemented adapter packages with tests, correct peerDependencies, and passing build + typecheck + attw.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adapters-docs-and-release/06-RESEARCH.md
@packages/sdk/src/client/HeyLolClient.ts
@packages/sdk/src/client/options.ts
@packages/adapter-cloudflare/package.json
@packages/adapter-cloudflare/tsup.config.ts
@packages/adapter-vercel/package.json
@packages/adapter-vercel/tsup.config.ts
@packages/adapter-express/package.json
@packages/adapter-express/tsup.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Express adapter with middleware factory and tests</name>
  <files>
    packages/adapter-express/src/index.ts
    packages/adapter-express/package.json
    packages/adapter-express/tsup.config.ts
    packages/adapter-express/tests/express.test.ts
  </files>
  <action>
    1. **package.json**: Add `@heylol/sdk` as peerDependency (`"workspace:*"`), add `express` as peerDependency (`">=4.0.0"`), add `@types/express` as devDependency (`"^5.0.6"`), add `@heylol/sdk` to devDependencies (`"workspace:*"`) for build. Keep existing devDeps.

    2. **tsup.config.ts**: Add `external: ['@heylol/sdk', 'express']` to prevent bundling peer deps.

    3. **src/index.ts**: Replace the VERSION stub with the full Express adapter:
       - Import `HeyLolClient` and `ClientOptions` from `@heylol/sdk`
       - Import Express types: `RequestHandler`, `Request`, `Response`, `NextFunction` from `express` (type-only import)
       - Declare global namespace augmentation: `declare global { namespace Express { interface Request { heyLolClient?: HeyLolClient; } } }`
       - Export `HeyLolMiddlewareOptions` interface: extends `Omit<ClientOptions, 'privateKey'>` with optional `privateKey?: string`
       - Export `createHeyLolMiddleware(opts?: HeyLolMiddlewareOptions): RequestHandler` factory:
         - Resolves privateKey from `opts?.privateKey ?? process.env.HEYLOL_PRIVATE_KEY`
         - Throws `Error('privateKey option or HEYLOL_PRIVATE_KEY env var is required')` if missing
         - Creates single `HeyLolClient` instance (stateless, safe to reuse across requests)
         - Returns `RequestHandler` that sets `req.heyLolClient = client` and calls `next()`
       - Add JSDoc with `@example` on `createHeyLolMiddleware` showing Express usage pattern
       - Include full TSDoc: `@param`, `@returns`, `@throws`, `@example`

    4. **tests/express.test.ts**: Create test suite:
       - Test: `createHeyLolMiddleware()` throws when no privateKey provided and env var unset
       - Test: `createHeyLolMiddleware({ privateKey: '...' })` returns a function
       - Test: middleware attaches `heyLolClient` to `req` and calls `next()`
       - Test: `createHeyLolMiddleware()` reads from `process.env.HEYLOL_PRIVATE_KEY` when no option
       - Use vi.stubGlobal for process.env mocking
       - Mock HeyLolClient constructor — do NOT actually import crypto deps (use vi.mock('@heylol/sdk'))
       - Create minimal req/res/next mocks (req as `{ heyLolClient?: unknown }`, next as vi.fn())
  </action>
  <verify>
    `pnpm --filter @heylol/adapter-express build && pnpm --filter @heylol/adapter-express typecheck` passes.
    `pnpm --filter @heylol/adapter-express test` passes with all tests green.
  </verify>
  <done>Express middleware factory creates a RequestHandler that attaches HeyLolClient to req, reads privateKey from option or env var, throws on missing key, and all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Cloudflare and Vercel adapters with tests</name>
  <files>
    packages/adapter-cloudflare/src/index.ts
    packages/adapter-cloudflare/package.json
    packages/adapter-cloudflare/tsup.config.ts
    packages/adapter-cloudflare/tests/cloudflare.test.ts
    packages/adapter-vercel/src/index.ts
    packages/adapter-vercel/package.json
    packages/adapter-vercel/tsup.config.ts
    packages/adapter-vercel/tests/vercel.test.ts
  </files>
  <action>
    **Cloudflare Adapter:**

    1. **package.json**: Add `@heylol/sdk` as peerDependency (`"workspace:*"`), add `@heylol/sdk` to devDependencies (`"workspace:*"`), add `@cloudflare/workers-types` to devDependencies. Keep existing devDeps.

    2. **tsup.config.ts**: Add `external: ['@heylol/sdk']` to prevent bundling.

    3. **src/index.ts**: Replace VERSION stub:
       - Import `HeyLolClient` and `ClientOptions` from `@heylol/sdk`
       - Export `HeyLolEnv` interface: `{ HEYLOL_PRIVATE_KEY: string; HEYLOL_BASE_URL?: string }`
       - Export `CloudflareClient` class extending `HeyLolClient`:
         - Constructor takes `(env: HeyLolEnv, opts?: Omit<ClientOptions, 'privateKey'>)`
         - Calls `super({ privateKey: env.HEYLOL_PRIVATE_KEY, baseUrl: env.HEYLOL_BASE_URL, ...opts })`
       - Export `createFromEnv(env: HeyLolEnv, opts?): HeyLolClient` factory function
       - JSDoc with `@example` blocks on both `CloudflareClient` and `createFromEnv`

    4. **tests/cloudflare.test.ts**: Tests using standard vitest (NOT @cloudflare/vitest-pool-workers — that would require complex setup for a thin wrapper that only calls super()):
       - Mock `@heylol/sdk` module (vi.mock) to avoid crypto dep chain
       - Test: `CloudflareClient` passes env.HEYLOL_PRIVATE_KEY to super
       - Test: `CloudflareClient` passes env.HEYLOL_BASE_URL to super when provided
       - Test: `createFromEnv` returns instance with correct config
       - Test: `CloudflareClient` instance has posts/profile/social resource namespaces (instanceof check)

    **Vercel Adapter:**

    1. **package.json**: Add `@heylol/sdk` as peerDependency (`"workspace:*"`), add `@heylol/sdk` to devDependencies (`"workspace:*"`). Add `@vercel/edge-config` as optionalDependency (for Next.js middleware helper), add `next` to devDependencies for types only. Keep existing devDeps.

    2. **tsup.config.ts**: Add `external: ['@heylol/sdk', '@vercel/edge-config', 'next', 'next/server']` to prevent bundling.

    3. **src/index.ts**: Replace VERSION stub:
       - Import `HeyLolClient` and `ClientOptions` from `@heylol/sdk`
       - Export `VercelClient` class extending `HeyLolClient`:
         - Constructor takes `(opts?: Omit<ClientOptions, 'privateKey'>)`
         - Reads `process.env.HEYLOL_PRIVATE_KEY`, throws Error if missing
         - Calls `super({ privateKey, ...opts })`
       - Export `NextjsMiddlewareOptions` interface: `{ edgeConfigConnectionString?: string; matcher?: string[] }`
       - Export `createNextjsMiddleware(opts?: NextjsMiddlewareOptions)` function:
         - Returns `async function middleware(request: NextRequest): Promise<NextResponse>`
         - Try-catch wraps `@vercel/edge-config` createClient call — falls back to process.env if Edge Config is unavailable (Pitfall 3 from research)
         - Sets `x-heylol-ready: 1` header on request for downstream route handlers
         - Returns `NextResponse.next({ request: { headers: requestHeaders } })`
       - JSDoc with `@example` blocks on both `VercelClient` and `createNextjsMiddleware`

    4. **tests/vercel.test.ts**: Tests:
       - Mock `@heylol/sdk` module
       - Test: `VercelClient` reads from process.env.HEYLOL_PRIVATE_KEY
       - Test: `VercelClient` throws when env var missing
       - Test: `VercelClient` accepts additional ClientOptions
       - Test: `createNextjsMiddleware` returns a function

    5. **CI validation**: Run `pnpm turbo build typecheck` to confirm all three adapter packages build and typecheck. Run `pnpm turbo test` to confirm all tests pass including the new adapter tests.
  </action>
  <verify>
    `pnpm turbo build typecheck` succeeds for all packages including all three adapters.
    `pnpm turbo test` passes with all adapter tests green.
    Each adapter's `dist/` contains `.mjs`, `.cjs`, `.d.ts`, `.d.cts` files.
  </verify>
  <done>CloudflareClient extends HeyLolClient with env binding support, VercelClient reads from process.env with optional Edge Config, createNextjsMiddleware returns a Next.js middleware function, all builds and tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build typecheck test` passes for all packages including all three adapters
2. Each adapter package has correct peerDependencies in package.json
3. Each adapter's tsup config externalizes @heylol/sdk
4. CloudflareClient, VercelClient, and createHeyLolMiddleware all compile and test
5. Adapter packages produce valid dist/ outputs with .mjs, .cjs, .d.ts, .d.cts
</verification>

<success_criteria>
- All three adapter packages build without errors
- All adapter tests pass
- `pnpm turbo typecheck` succeeds across the full workspace
- Each adapter externalizes @heylol/sdk (not bundled)
</success_criteria>

<output>
After completion, create `.planning/phases/06-adapters-docs-and-release/06-01-SUMMARY.md`
</output>
