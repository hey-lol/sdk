---
phase: 06-adapters-docs-and-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/src/client/HeyLolClient.ts
  - packages/sdk/src/resources/PostsResource.ts
  - packages/sdk/src/resources/ProfileResource.ts
  - packages/sdk/src/resources/SocialResource.ts
  - packages/sdk/src/resources/DiscoveryResource.ts
  - packages/sdk/src/resources/NotificationsResource.ts
  - packages/sdk/src/resources/ServicesResource.ts
  - packages/services/src/register.ts
  - packages/services/src/response.ts
  - packages/services/src/verify.ts
  - packages/services/src/settle.ts
  - packages/services/src/handler.ts
autonomous: true
requirements:
  - TYPE-04

must_haves:
  truths:
    - "Every public method on HeyLolClient has JSDoc with @param, @returns, @throws, and @example"
    - "Every public method on all 6 Resource classes has JSDoc with @param, @returns, and @example"
    - "Every exported function in @heylol/services has JSDoc with @param, @returns, and @example"
    - "All @example blocks use correct method names that exist in the codebase"
    - "Build and typecheck still pass after JSDoc additions"
  artifacts:
    - path: "packages/sdk/src/client/HeyLolClient.ts"
      provides: "JSDoc on constructor, request, get, post, patch, delete"
      contains: "@example"
    - path: "packages/sdk/src/resources/PostsResource.ts"
      provides: "JSDoc on create, get, delete, like, unlike, reply"
      contains: "@example"
    - path: "packages/services/src/handler.ts"
      provides: "JSDoc on createX402Service"
      contains: "@example"
  key_links:
    - from: "JSDoc @example blocks"
      to: "actual method signatures"
      via: "method names and parameter types must match"
      pattern: "client\\.posts\\."
---

<objective>
Add comprehensive JSDoc/TSDoc documentation to all public methods across the SDK and services packages: HeyLolClient, all 6 Resource classes, and all @heylol/services exports.

Purpose: IDE hover documentation gives developers immediate understanding of every method with copy-pastable examples, satisfying TYPE-04 (JSDoc comments with examples on all public methods).

Output: Every public method has `@param`, `@returns`, `@throws` (where applicable), and `@example` TSDoc blocks.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adapters-docs-and-release/06-RESEARCH.md
@packages/sdk/src/client/HeyLolClient.ts
@packages/sdk/src/client/options.ts
@packages/sdk/src/resources/PostsResource.ts
@packages/sdk/src/resources/ProfileResource.ts
@packages/sdk/src/resources/SocialResource.ts
@packages/sdk/src/resources/DiscoveryResource.ts
@packages/sdk/src/resources/NotificationsResource.ts
@packages/sdk/src/resources/ServicesResource.ts
@packages/services/src/register.ts
@packages/services/src/response.ts
@packages/services/src/verify.ts
@packages/services/src/settle.ts
@packages/services/src/handler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSDoc to HeyLolClient and all Resource classes</name>
  <files>
    packages/sdk/src/client/HeyLolClient.ts
    packages/sdk/src/resources/PostsResource.ts
    packages/sdk/src/resources/ProfileResource.ts
    packages/sdk/src/resources/SocialResource.ts
    packages/sdk/src/resources/DiscoveryResource.ts
    packages/sdk/src/resources/NotificationsResource.ts
    packages/sdk/src/resources/ServicesResource.ts
  </files>
  <action>
    Add TSDoc `/** */` blocks to every public method. Do NOT duplicate TypeScript types in JSDoc — TypeScript infers them. Focus on summary, `@param` descriptions, `@returns`, `@throws`, and `@example`.

    **HeyLolClient** (packages/sdk/src/client/HeyLolClient.ts):
    - Class-level JSDoc: summary, `@example` showing basic initialization
    - `constructor(opts)`: `@param opts` description, `@throws` for invalid key, `@example`
    - `request<T>(method, path, body?)`: already has internal docs — add `@param`, `@returns`, `@throws` for all error types
    - `get<T>(path, params?)`: `@param`, `@returns`, `@example` showing `client.get<Post[]>('/posts')`
    - `post<T>(path, body?)`: `@param`, `@returns`, `@example`
    - `patch<T>(path, body?)`: `@param`, `@returns`, `@example`
    - `delete<T>(path)`: `@param`, `@returns`, `@example`

    **PostsResource** (packages/sdk/src/resources/PostsResource.ts):
    - Expand existing JSDoc on each method to include `@param`, `@returns`, `@throws`, `@example`:
    - `create(params)`: example with text post and media post (already has partial JSDoc — enhance with `@example`)
    - `get(id)`: example: `const post = await client.posts.get(asPostId('abc123'));`
    - `delete(id)`: example: `await client.posts.delete(asPostId('abc123'));`
    - `like(id)` / `unlike(id)`: examples
    - `reply(id, params)`: example with reply content

    **ProfileResource** (packages/sdk/src/resources/ProfileResource.ts):
    - `me()`: `@returns` own profile, `@example`: `const profile = await client.profile.me();`
    - `get(id)`: `@example`: `const profile = await client.profile.get(asUserId('user123'));`
    - `update(params)`: `@example` showing updating display name and avatar

    **SocialResource** (packages/sdk/src/resources/SocialResource.ts):
    - `follow(id)`: `@example`: `await client.social.follow(asUserId('user123'));`
    - `unfollow(id)`: example
    - `followers(id, params?)`: `@example` with pagination
    - `following(id, params?)`: example

    **DiscoveryResource** (packages/sdk/src/resources/DiscoveryResource.ts):
    - `search(params)`: `@example` with query and type filter
    - `trending(params?)`: example
    - `suggested(params?)`: example

    **NotificationsResource** (packages/sdk/src/resources/NotificationsResource.ts):
    - `list(params?)`: example with pagination
    - `markRead(ids?)`: example marking specific and all

    **ServicesResource** (packages/sdk/src/resources/ServicesResource.ts):
    - `call<TInput, TOutput>(serviceId, input?)`: enhance existing JSDoc with `@example`

    **CRITICAL**: Every `@example` must use correct method names. Use `client.posts.create()` not `client.post.create()`. Use `asPostId()` not `PostId()`. Verify against actual exports in `packages/sdk/src/index.ts`.
  </action>
  <verify>
    `pnpm --filter @heylol/sdk build && pnpm --filter @heylol/sdk typecheck` passes.
    `pnpm --filter @heylol/sdk test` still passes (JSDoc-only changes should not break tests).
    Grep for `@example` in all modified files confirms every public method has one.
  </verify>
  <done>Every public method in HeyLolClient and all 6 Resource classes has TSDoc with @param, @returns, @throws (where applicable), and @example blocks using correct method names.</done>
</task>

<task type="auto">
  <name>Task 2: Add JSDoc to all @heylol/services exports</name>
  <files>
    packages/services/src/register.ts
    packages/services/src/response.ts
    packages/services/src/verify.ts
    packages/services/src/settle.ts
    packages/services/src/handler.ts
  </files>
  <action>
    Add TSDoc `/** */` blocks to every exported function and interface in the services package.

    **registerService** (register.ts):
    - Function JSDoc: "Register a service definition with price, schema, and metadata."
    - `@param opts` — describe each field
    - `@returns` typed `ServiceDefinition`
    - `@example` showing registration with price config and Zod schema
    - `RegisterServiceOptions` interface: JSDoc on each field

    **create402Response** (response.ts):
    - Function JSDoc: "Generate a 402 Payment Required response with x402 v2 payment requirements."
    - `@param definition` — the service definition
    - `@param opts` — optional resource URL and description override
    - `@returns` a `Response` with status 402 and PAYMENT-REQUIRED header
    - `@example` showing basic usage
    - `Create402Options` interface: JSDoc on each field
    - `USDC_MINT` constant: JSDoc explaining it maps CAIP-2 network IDs to USDC token addresses

    **verifyPayment** (verify.ts):
    - Function JSDoc: "Verify an incoming x402 payment header against a facilitator."
    - `@param paymentSignatureHeader` — base64-encoded payment signature from request header
    - `@param requirements` — payment requirements to verify against
    - `@param facilitatorUrl` — optional facilitator URL (defaults to x402.org)
    - `@returns` `VerifyResult` — never throws, returns `{ valid: false }` on error
    - `@example`

    **settlePayment** (settle.ts):
    - Function JSDoc: "Settle a verified payment on-chain via the facilitator."
    - `@param paymentPayload` — the payment payload from verification
    - `@param requirements` — the payment requirements used for verification
    - `@param facilitatorUrl` — optional facilitator URL
    - `@returns` `SettleResult` — never throws, returns `{ success: false }` on error
    - `@example`

    **createX402Service** (handler.ts):
    - Function JSDoc: "Create a complete x402 service handler that bundles verification, handler execution, and settlement."
    - `@param definition` — service definition from registerService
    - `@param handler` — async function `(request, input) => output`
    - `@param opts` — optional facilitator URL
    - `@returns` `(request: Request) => Promise<Response>` — a fetch-compatible handler
    - `@example` showing full service setup with registerService + createX402Service
    - `ServiceHandler` type: JSDoc
    - `X402ServiceOptions` interface: JSDoc on each field

    **CRITICAL**: Examples must use actual export names from `packages/services/src/index.ts`: `registerService`, `create402Response`, `verifyPayment`, `settlePayment`, `createX402Service`, `USDC_MINT`.

    Run `pnpm --filter @heylol/services build && pnpm --filter @heylol/services typecheck && pnpm --filter @heylol/services test` to verify.
  </action>
  <verify>
    `pnpm --filter @heylol/services build && pnpm --filter @heylol/services typecheck` passes.
    `pnpm --filter @heylol/services test` still passes.
    Grep for `@example` in all 5 modified service files confirms coverage.
  </verify>
  <done>Every exported function and interface in @heylol/services has TSDoc with @param, @returns, and @example blocks using correct export names.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build typecheck test` passes across the full workspace
2. Every public method in HeyLolClient, all 6 Resource classes, and all @heylol/services exports has `@example` in its JSDoc
3. No @example block references a method name that doesn't exist
4. JSDoc additions do not break any existing tests
</verification>

<success_criteria>
- grep -r "@example" across all modified files shows coverage on every public method
- `pnpm turbo build typecheck test` succeeds
- IDE hover on any public method shows summary, params, returns, and example
</success_criteria>

<output>
After completion, create `.planning/phases/06-adapters-docs-and-release/06-02-SUMMARY.md`
</output>
