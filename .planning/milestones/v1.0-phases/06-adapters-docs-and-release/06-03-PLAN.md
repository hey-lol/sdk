---
phase: 06-adapters-docs-and-release
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
  - examples/cloudflare-ai-agent/src/index.ts
  - examples/cloudflare-ai-agent/wrangler.jsonc
  - examples/cloudflare-ai-agent/package.json
  - examples/cloudflare-ai-agent/tsconfig.json
  - examples/x402-service-provider/src/index.ts
  - examples/x402-service-provider/package.json
  - examples/x402-service-provider/tsconfig.json
  - examples/nextjs-dashboard/app/page.tsx
  - examples/nextjs-dashboard/app/api/posts/route.ts
  - examples/nextjs-dashboard/middleware.ts
  - examples/nextjs-dashboard/package.json
  - examples/nextjs-dashboard/tsconfig.json
  - examples/nextjs-dashboard/next.config.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "A developer reads the README quickstart and understands install + init + first post in a linear flow"
    - "The cloudflare-ai-agent example has a valid wrangler.jsonc and src/index.ts using CloudflareClient"
    - "The x402-service-provider example shows registerService + createX402Service usage"
    - "The nextjs-dashboard example has a route handler using VercelClient and middleware using createNextjsMiddleware"
    - "All example projects typecheck without errors"
    - "pnpm publish --dry-run succeeds for SDK and all adapter packages"
  artifacts:
    - path: "README.md"
      provides: "5-minute quickstart: install, init, first post"
      contains: "npm install @heylol/sdk"
    - path: "examples/cloudflare-ai-agent/src/index.ts"
      provides: "Cloudflare Worker entry point using CloudflareClient"
      contains: "createFromEnv"
    - path: "examples/x402-service-provider/src/index.ts"
      provides: "x402 service using registerService + createX402Service"
      contains: "createX402Service"
    - path: "examples/nextjs-dashboard/app/api/posts/route.ts"
      provides: "Next.js route handler using VercelClient"
      contains: "VercelClient"
  key_links:
    - from: "README.md quickstart"
      to: "@heylol/sdk"
      via: "npm install + HeyLolClient import"
      pattern: "import.*HeyLolClient.*from.*@heylol/sdk"
    - from: "examples/cloudflare-ai-agent"
      to: "@heylol/adapter-cloudflare"
      via: "package.json dependency + import"
      pattern: "@heylol/adapter-cloudflare"
    - from: "examples/nextjs-dashboard"
      to: "@heylol/adapter-vercel"
      via: "package.json dependency + import"
      pattern: "@heylol/adapter-vercel"
---

<objective>
Create the README quickstart documentation and three example projects that demonstrate real-world SDK usage across Cloudflare Workers, x402 service providers, and Next.js dashboards.

Purpose: Any developer can go from npm install to first API call in under 5 minutes following the README. Example projects serve as both documentation and integration validation of the adapter packages.

Output: README.md with quickstart, three example projects under `examples/`, all typechecking, and validated publish pipeline.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adapters-docs-and-release/06-RESEARCH.md
@.planning/phases/06-adapters-docs-and-release/06-01-SUMMARY.md
@packages/sdk/src/index.ts
@packages/services/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README quickstart and example projects</name>
  <files>
    README.md
    examples/cloudflare-ai-agent/src/index.ts
    examples/cloudflare-ai-agent/wrangler.jsonc
    examples/cloudflare-ai-agent/package.json
    examples/cloudflare-ai-agent/tsconfig.json
    examples/x402-service-provider/src/index.ts
    examples/x402-service-provider/package.json
    examples/x402-service-provider/tsconfig.json
    examples/nextjs-dashboard/app/page.tsx
    examples/nextjs-dashboard/app/api/posts/route.ts
    examples/nextjs-dashboard/middleware.ts
    examples/nextjs-dashboard/package.json
    examples/nextjs-dashboard/tsconfig.json
    examples/nextjs-dashboard/next.config.ts
  </files>
  <action>
    **README.md** (project root):
    Create a comprehensive README with these sections in order:
    1. **Title + tagline**: "@heylol/sdk — The official hey.lol SDK for TypeScript/JavaScript"
    2. **Badges**: npm version, TypeScript, license MIT
    3. **Features**: bullet list — x402 auth handled automatically, pure JS crypto (no Node.js built-ins), works in Node.js 18+, Cloudflare Workers, Vercel Edge, browsers, typed API surface with branded IDs
    4. **Quick Start** (must be completable in under 5 minutes reading):
       - Install: `npm install @heylol/sdk`
       - Init: `import { HeyLolClient } from '@heylol/sdk'; const client = new HeyLolClient({ privateKey: 'your_base58_key' });`
       - First post: `const post = await client.posts.create({ content: 'Hello, hey.lol!' });`
       - Complete runnable example (6-8 lines total)
    5. **Platform Adapters**: brief section showing Cloudflare, Vercel, Express usage (3-5 lines each). Install commands: `npm install @heylol/adapter-cloudflare` etc.
    6. **API Overview**: table of all resource namespaces (posts, profile, social, discovery, notifications, services) with 1-line method summaries
    7. **Services Package**: brief section on `@heylol/services` for service providers (registerService, createX402Service)
    8. **Examples**: links to the three example projects in `examples/`
    9. **Error Handling**: brief section showing try/catch with typed errors (AuthError, RateLimitError, APIError, NetworkError)
    10. **License**: MIT

    **CRITICAL README RULES**:
    - Show `HeyLolClient` directly in quickstart (no adapter needed for Node.js) per research recommendation
    - All code blocks must use correct import paths and method names
    - Keep quickstart linear — no branching, no "if you're using X instead..."
    - Adapters are progressive disclosure (separate section after quickstart)

    **Example: cloudflare-ai-agent** (DOCS-02):
    - `package.json`: name `heylol-cloudflare-ai-agent`, private: true, type: module, deps: `@heylol/adapter-cloudflare: "workspace:*"`, devDeps: `wrangler`, `@cloudflare/workers-types`, `typescript`. Scripts: `"dev": "wrangler dev"`, `"build": "tsc --noEmit"`.
    - `tsconfig.json`: extends `../../tsconfig.json`, add `@cloudflare/workers-types` to `types` array, include `src/**/*.ts`
    - `wrangler.jsonc`: `$schema`, name `heylol-ai-agent`, main `src/index.ts`, compatibility_date `2026-03-01`. Comment about `wrangler secret put HEYLOL_PRIVATE_KEY` and `.dev.vars`.
    - `src/index.ts`: Worker entry point using `createFromEnv` from `@heylol/adapter-cloudflare`. Defines `Env` interface extending `HeyLolEnv`. fetch handler creates client, creates a post, returns Response.json. `export default { fetch } satisfies ExportedHandler<Env>`.

    **Example: x402-service-provider** (DOCS-03):
    - `package.json`: name `heylol-x402-service-provider`, private: true, type: module, deps: `@heylol/services: "workspace:*"`, devDeps: `typescript`. Scripts: `"build": "tsc --noEmit"`.
    - `tsconfig.json`: extends `../../tsconfig.json`, include `src/**/*.ts`
    - `src/index.ts`: Demonstrates the full x402 service lifecycle:
      1. `registerService({ id: 'ai-summary', price: { amount: '0.01', network: 'solana:...', payTo: '...' } })`
      2. Define handler: `async (req, input) => ({ summary: '...' })`
      3. `createX402Service(definition, handler)`
      4. Export the handler or show how to use with a server (Cloudflare Worker or Bun.serve)

    **Example: nextjs-dashboard** (DOCS-04):
    - `package.json`: name `heylol-nextjs-dashboard`, private: true, type: module, deps: `@heylol/adapter-vercel: "workspace:*"`, `next`, `react`, `react-dom`. devDeps: `typescript`, `@types/react`, `@types/react-dom`. Scripts: `"dev": "next dev"`, `"build": "next build"`.
    - `tsconfig.json`: standard Next.js tsconfig with jsx: preserve, module: esnext, moduleResolution: bundler, paths for `@/*`
    - `next.config.ts`: minimal — `export default {}`
    - `middleware.ts`: imports `createNextjsMiddleware` from `@heylol/adapter-vercel`, exports middleware and config matcher
    - `app/api/posts/route.ts`: route handler with `export const runtime = 'edge'`, uses `VercelClient` to create a post
    - `app/page.tsx`: minimal server component showing a "Dashboard" heading (does not need full UI — this is a code example, not a production app)

    **CRITICAL EXAMPLE RULES**:
    - All examples must have `"private": true` to prevent accidental publish
    - All examples must have `"build": "tsc --noEmit"` (or `"next build"` for Next.js) so `turbo build` works
    - Use `workspace:*` for local package references
    - Do NOT include `.dev.vars` files or any secrets
    - Ensure `pnpm-workspace.yaml` already includes `examples/*` (it does)
  </action>
  <verify>
    `pnpm install` succeeds with new example packages detected.
    `pnpm --filter heylol-cloudflare-ai-agent build` (tsc --noEmit) passes.
    `pnpm --filter heylol-x402-service-provider build` (tsc --noEmit) passes.
    README.md exists and contains "Quick Start" section with install + init + first post.
  </verify>
  <done>README quickstart enables npm install to first API call in under 5 minutes. Three example projects demonstrate Cloudflare Worker, x402 service provider, and Next.js dashboard patterns. All examples typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Validate publish pipeline and run final CI checks</name>
  <files>
    packages/adapter-cloudflare/package.json
    packages/adapter-vercel/package.json
    packages/adapter-express/package.json
  </files>
  <action>
    1. Run full CI pipeline: `pnpm turbo build typecheck test`
    2. Run `attw --pack . --ignore-rules cjs-resolves-to-esm` in each adapter package directory to validate TypeScript declarations resolve correctly across all module resolution modes (node10, node16 ESM/CJS, bundler).
    3. Run `pnpm --filter @heylol/sdk publish --dry-run` to verify SDK publish readiness.
    4. Run `pnpm --filter @heylol/adapter-cloudflare publish --dry-run`, `pnpm --filter @heylol/adapter-vercel publish --dry-run`, `pnpm --filter @heylol/adapter-express publish --dry-run` to verify adapter publish readiness.
    5. Run `pnpm --filter @heylol/services publish --dry-run` to verify services package.
    6. If attw reports errors on any adapter package:
       - Check that `@heylol/sdk` is in `peerDependencies` (Pitfall 5 from research)
       - Check that tsup config has `external: ['@heylol/sdk']`
       - Fix and re-run
    7. Run `pnpm turbo size-check` to confirm bundle sizes still within limits.
    8. Verify all tests pass: `pnpm turbo test` — capture total test count.
  </action>
  <verify>
    `pnpm turbo build typecheck test` all green.
    `attw --pack .` passes for all 5 publishable packages (sdk, services, adapter-cloudflare, adapter-vercel, adapter-express) with zero errors (cjs-resolves-to-esm ignored per Phase 1 convention).
    `pnpm publish --dry-run` passes for all 5 packages.
    Size check passes.
  </verify>
  <done>Full CI pipeline green: build, typecheck, test, size-check, attw, and publish dry-run all pass for every package. SDK is publish-ready.</done>
</task>

</tasks>

<verification>
1. README.md quickstart: install + init + first post in linear reading under 5 minutes
2. Three example projects exist under examples/ and typecheck
3. `pnpm turbo build typecheck test` all green
4. `attw --pack .` passes for all 5 publishable packages
5. `pnpm publish --dry-run` passes for all 5 packages
6. Total test count reported (should be 165+ from Phase 5 plus new adapter tests)
</verification>

<success_criteria>
- README quickstart is linear: install, init, first post — no branching
- All three example projects typecheck without errors
- All 5 packages pass attw and publish dry-run
- Full CI pipeline (build, typecheck, test, size-check) green
</success_criteria>

<output>
After completion, create `.planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md`
</output>
