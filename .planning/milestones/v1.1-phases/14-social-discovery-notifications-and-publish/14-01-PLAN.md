---
phase: 14-social-discovery-notifications-and-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/commands/social.ts
  - packages/cli/src/commands/discovery.ts
  - packages/cli/src/commands/notifications.ts
autonomous: true
requirements:
  - SOCL-01
  - SOCL-02
  - SOCL-03
  - SOCL-04
  - DISC-01
  - DISC-02
  - DISC-03
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "`heylol social follow <id>` calls SDK social.follow and exits cleanly"
    - "`heylol social unfollow <id>` calls SDK social.unfollow and exits cleanly"
    - "`heylol social followers <id>` returns paginated JSON with nextCursor"
    - "`heylol social following <id>` returns paginated JSON with nextCursor"
    - "`heylol discovery search --query \"ai\"` returns search results JSON"
    - "`heylol discovery trending` returns paginated list without arguments"
    - "`heylol discovery suggested` returns paginated list without arguments"
    - "`heylol notifications list` returns paginated notifications JSON"
    - "`heylol notifications mark-read` completes without error"
  artifacts:
    - path: "packages/cli/src/commands/social.ts"
      provides: "follow, unfollow, followers, following command handlers"
      contains: "asUserId"
    - path: "packages/cli/src/commands/discovery.ts"
      provides: "search, trending, suggested command handlers"
      contains: "client.discovery.search"
    - path: "packages/cli/src/commands/notifications.ts"
      provides: "list, mark-read command handlers"
      contains: "client.notifications.markRead"
  key_links:
    - from: "packages/cli/src/commands/social.ts"
      to: "@heylol/sdk SocialResource"
      via: "createClient(opts).social.*"
      pattern: "client\\.social\\.(follow|unfollow|followers|following)"
    - from: "packages/cli/src/commands/discovery.ts"
      to: "@heylol/sdk DiscoveryResource"
      via: "createClient(opts).discovery.*"
      pattern: "client\\.discovery\\.(search|trending|suggested)"
    - from: "packages/cli/src/commands/notifications.ts"
      to: "@heylol/sdk NotificationsResource"
      via: "createClient(opts).notifications.*"
      pattern: "client\\.notifications\\.(list|markRead)"
---

<objective>
Wire all 9 remaining CLI command stubs (social follow/unfollow/followers/following, discovery search/trending/suggested, notifications list/mark-read) to their corresponding @heylol/sdk resource methods, replacing `throw new Error('not implemented')` with real action handlers.

Purpose: Complete the CLI's API surface so every hey.lol action is accessible via single-line commands.
Output: Three fully implemented command files that pass typecheck and build.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-social-discovery-notifications-and-publish/14-RESEARCH.md

# Phase 13 posts.ts is the canonical template for all command patterns
@packages/cli/src/commands/posts.ts
@packages/cli/src/commands/profile.ts
@packages/cli/src/config.ts
@packages/cli/src/context.ts
@packages/cli/src/output.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire social, discovery, and notifications command handlers</name>
  <files>
    packages/cli/src/commands/social.ts
    packages/cli/src/commands/discovery.ts
    packages/cli/src/commands/notifications.ts
  </files>
  <action>
Replace the entire contents of each stub file with fully wired implementations. Use `posts.ts` as the exact template for every pattern.

**social.ts** — Replace entire file:
- Add imports: `{ asUserId }` from `@heylol/sdk`, `{ Command }` from `commander`, `{ createClient }` from `../config.js`, `type { GlobalContext }` from `../context.js`, `{ printFailure, printSuccess }` from `../output.js`
- `follow` handler: `async function (this: Command, id: string)` — `await client.social.follow(asUserId(id))` then `printSuccess(null, opts)` (void pattern)
- `unfollow` handler: same void pattern with `client.social.unfollow(asUserId(id))`
- `followers` handler: `async function (this: Command, id: string)` with `opts` typed as `GlobalContext & { cursor?: string; limit?: number }` — `const result = await client.social.followers(asUserId(id), { cursor: opts.cursor, limit: opts.limit })` then `printSuccess(result, opts)`
- `following` handler: same paginated pattern with `client.social.following(asUserId(id), ...)`

**discovery.ts** — Replace entire file:
- Add imports: `{ Command }` from `commander`, `{ createClient }` from `../config.js`, `type { GlobalContext }` from `../context.js`, `{ printFailure, printSuccess }` from `../output.js`
- NO `asUserId` needed — discovery commands have no user ID arguments
- `search` handler: `async function (this: Command)` (no positional args) with `opts` typed as `GlobalContext & { query: string; cursor?: string; limit?: number }` — `const result = await client.discovery.search({ query: opts.query, cursor: opts.cursor, limit: opts.limit })` then `printSuccess(result, opts)`
- `trending` handler: `async function (this: Command)` with `opts` typed as `GlobalContext & { cursor?: string; limit?: number }` — `const result = await client.discovery.trending({ cursor: opts.cursor, limit: opts.limit })` then `printSuccess(result, opts)`
- `suggested` handler: same paginated pattern with `client.discovery.suggested(...)`

**notifications.ts** — Replace entire file:
- Add imports: `{ Command }` from `commander`, `{ createClient }` from `../config.js`, `type { GlobalContext }` from `../context.js`, `{ printFailure, printSuccess }` from `../output.js`
- `list` handler: `async function (this: Command)` (no positional args) with `opts` typed as `GlobalContext & { cursor?: string; limit?: number }` — `const result = await client.notifications.list({ cursor: opts.cursor, limit: opts.limit })` then `printSuccess(result, opts)`
- `mark-read` handler: `async function (this: Command)` with `opts` typed as `GlobalContext` — `await client.notifications.markRead()` then `printSuccess(null, opts)` (void pattern, no --ids flag per research recommendation)

**Critical patterns to follow (from posts.ts):**
- MUST use `async function (this: Command, ...)` — NOT arrow functions (loses `this` binding)
- MUST use `this.optsWithGlobals<T>()` to access options
- Positional arguments (`<id>`) come as function params, NOT from opts
- MUST wrap string IDs: `asUserId(id)` for branded type safety
- MUST pass `null` (not undefined) to `printSuccess` for void commands
- Every handler wraps in try/catch with `printFailure(err, opts)` in catch
- All existing command/argument/option declarations stay exactly as they are in stubs — only the `.action(...)` callback bodies change

The complete replacement code for all three files is provided in the research file at `.planning/phases/14-social-discovery-notifications-and-publish/14-RESEARCH.md` under "Code Examples". Use those verbatim.
  </action>
  <verify>
Run from repo root:
1. `pnpm typecheck --filter heylol` — must pass with zero errors
2. `pnpm build --filter heylol` — must succeed, producing `dist/cli.mjs`
3. `node packages/cli/dist/cli.mjs social --help` — must show follow/unfollow/followers/following subcommands
4. `node packages/cli/dist/cli.mjs discovery --help` — must show search/trending/suggested subcommands
5. `node packages/cli/dist/cli.mjs notifications --help` — must show list/mark-read subcommands
6. Grep all three files for `throw new Error('not implemented')` — must return zero matches
  </verify>
  <done>All 9 command stubs replaced with SDK-wired action handlers. TypeScript compiles cleanly. Built CLI shows all subcommands in --help. No "not implemented" errors remain in social.ts, discovery.ts, or notifications.ts.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck --filter heylol` passes
2. `pnpm build --filter heylol` succeeds
3. `grep -r "not implemented" packages/cli/src/commands/social.ts packages/cli/src/commands/discovery.ts packages/cli/src/commands/notifications.ts` returns empty
4. `node packages/cli/dist/cli.mjs social follow --help` shows usage with `<id>` argument
5. `node packages/cli/dist/cli.mjs discovery search --help` shows `--query` as required
6. `node packages/cli/dist/cli.mjs notifications mark-read --help` shows no required arguments
</verification>

<success_criteria>
- All 9 command handlers call real SDK methods (not stubs)
- TypeScript compiles with zero errors
- CLI builds and all --help outputs are correct
- Void commands (follow, unfollow, mark-read) pass null to printSuccess
- Paginated commands (followers, following, trending, suggested, list) pass cursor/limit options
- Search command passes query as required option
- All ID arguments wrapped with asUserId() for type safety
</success_criteria>

<output>
After completion, create `.planning/phases/14-social-discovery-notifications-and-publish/14-01-SUMMARY.md`
</output>
