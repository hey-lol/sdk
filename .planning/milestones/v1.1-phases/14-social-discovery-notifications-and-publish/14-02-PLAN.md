---
phase: 14-social-discovery-notifications-and-publish
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified: []
autonomous: false
requirements:
  - SOCL-01
  - SOCL-02
  - SOCL-03
  - SOCL-04
  - DISC-01
  - DISC-02
  - DISC-03
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "`heylol` package is published to npm at version 1.0.0"
    - "`npx heylol --version` prints 1.0.0 from a clean directory with no local workspace"
    - "Published tarball contains dist/cli.mjs with real command implementations (not stubs)"
    - "`@heylol/sdk` dependency in published package.json shows `1.0.0` (not `workspace:*`)"
  artifacts: []
  key_links:
    - from: "published heylol@1.0.0 on npm"
      to: "@heylol/sdk@1.0.0 on npm"
      via: "runtime dependency in package.json"
      pattern: "@heylol/sdk.*1\\.0\\.0"
---

<objective>
Publish the `heylol` CLI package to npm so that `npx heylol --version` works from a clean directory. This is the final step of the v1.1 CLI milestone.

Purpose: Make the CLI available to all developers and AI agents via `npx heylol` without cloning the repo.
Output: `heylol@1.0.0` live on npm registry; verified via `npx heylol@1.0.0 --version`.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-social-discovery-notifications-and-publish/14-RESEARCH.md
@.planning/phases/14-social-discovery-notifications-and-publish/14-01-SUMMARY.md
@packages/cli/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run publish preflight</name>
  <files></files>
  <action>
Run the full build and publish preflight from the repo root:

1. **Rebuild everything:** `pnpm turbo build` — ensures dist/cli.mjs contains the real command implementations from Plan 14-01, not stale stubs. The turbo `build` task has `"dependsOn": ["^build"]` so it will rebuild @heylol/sdk first if needed.

2. **Verify no stubs remain in built output:** `grep "not implemented" packages/cli/dist/cli.mjs` — must return empty (zero matches). If it matches, the build picked up stale source files; re-run build.

3. **Run npm publish dry-run:** `cd packages/cli && npm publish --dry-run` — inspect output for:
   - No warnings or errors
   - Tarball includes `dist/cli.mjs` and `package.json`
   - Note: npm dry-run will show `workspace:*` for the SDK dependency — this is expected because npm doesn't understand pnpm workspace protocol. The real publish via `pnpm publish` rewrites it to `1.0.0`.

4. **Run pnpm publish dry-run:** `pnpm publish --filter heylol --dry-run --no-git-checks` — verify it completes without errors.

Do NOT actually publish yet — this task is preflight only.
  </action>
  <verify>
1. `pnpm turbo build` exits 0
2. `grep "not implemented" packages/cli/dist/cli.mjs` returns empty
3. `cd packages/cli && npm publish --dry-run` exits 0 with no warnings
4. `node packages/cli/dist/cli.mjs --version` prints `1.0.0`
  </verify>
  <done>CLI is rebuilt with all real command handlers, preflight checks pass, ready to publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Publish to npm and verify</name>
  <files></files>
  <action>
Publish the `heylol` CLI package to npm and verify it works from a clean install.

1. **Ensure npm auth:** Run `npm whoami` to confirm you are logged in. If not, the user must run `npm login` first.

2. **Publish via pnpm:** Run `pnpm publish --filter heylol --no-git-checks` from the repo root. This publishes `heylol@1.0.0` to npm with the `workspace:*` dependency on `@heylol/sdk` rewritten to `1.0.0`.

   **CRITICAL:** Use `pnpm publish` (NOT `npm publish`) to ensure the `workspace:*` protocol is rewritten to the real version. Using `npm publish` would publish the literal string `workspace:*` which breaks consumer installs.

3. **Verify from registry:** Run `npx heylol@1.0.0 --version` — should print `1.0.0`.

4. **Verify dependency rewriting:** Run `npm info heylol dependencies` — should show `@heylol/sdk: '1.0.0'` (not `workspace:*`).

**Human verification required:** User must confirm npm auth is active and approve the publish. After publish, user verifies `npx heylol@1.0.0 --version` works from a clean directory. Type "approved" to confirm or describe issues.
  </action>
  <verify>
1. `npm info heylol` returns package metadata showing version 1.0.0
2. `npm info heylol dependencies` shows `@heylol/sdk: 1.0.0` (not `workspace:*`)
3. `npx heylol@1.0.0 --version` prints `1.0.0`
4. `npx heylol@1.0.0 social --help` shows follow/unfollow/followers/following
  </verify>
  <done>heylol@1.0.0 is live on npm. `npx heylol@1.0.0 --version` prints 1.0.0 from a clean directory with no local workspace. Published package.json shows @heylol/sdk at 1.0.0 (not workspace:*).</done>
</task>

</tasks>

<verification>
1. `npm info heylol` returns package metadata showing version 1.0.0
2. `npm info heylol dependencies` shows `@heylol/sdk: 1.0.0` (not `workspace:*`)
3. `npx heylol@1.0.0 --version` prints `1.0.0` from a clean directory
4. `npx heylol@1.0.0 social --help` shows follow/unfollow/followers/following subcommands
5. `npx heylol@1.0.0 discovery --help` shows search/trending/suggested subcommands
6. `npx heylol@1.0.0 notifications --help` shows list/mark-read subcommands
</verification>

<success_criteria>
- `heylol@1.0.0` is live on npm registry
- `npx heylol@1.0.0 --version` prints 1.0.0 from a clean directory with no local workspace
- Published tarball's package.json shows `@heylol/sdk: "1.0.0"` (workspace:* rewritten by pnpm)
- All command groups (social, discovery, notifications) are functional in published version
</success_criteria>

<output>
After completion, create `.planning/phases/14-social-discovery-notifications-and-publish/14-02-SUMMARY.md`
</output>
