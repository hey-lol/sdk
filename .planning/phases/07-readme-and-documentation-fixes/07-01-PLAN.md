---
phase: 07-readme-and-documentation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "README error-handling code block uses err.retryAfterMs (not err.retryAfter) matching RateLimitError class"
    - "README error-handling code block uses err.statusCode (not err.status) matching APIError class"
    - "README API Overview table includes profile.me() and discovery.suggested()"
    - "06-03-SUMMARY.md frontmatter contains requirements-completed: [DOCS-02, DOCS-03, DOCS-04]"
  artifacts:
    - path: "README.md"
      provides: "Correct error-handling example code"
      contains: "err.retryAfterMs"
    - path: "README.md"
      provides: "Correct APIError usage"
      contains: "err.statusCode"
    - path: ".planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md"
      provides: "Phase 6 requirements completion record"
      contains: "requirements-completed"
  key_links:
    - from: "README.md"
      to: "packages/sdk/src/errors/index.ts"
      via: "Property name alignment"
      pattern: "retryAfterMs|statusCode"
---

<objective>
Fix two incorrect property names in the README error-handling code block and add missing requirements-completed frontmatter to 06-03-SUMMARY.md.

Purpose: A developer following the README quickstart should hit zero TypeScript errors. The current README references `err.retryAfter` and `err.status` which do not exist on the SDK error classes — the actual properties are `err.retryAfterMs` (RateLimitError) and `err.statusCode` (APIError). Additionally, the Phase 6 SUMMARY is missing frontmatter that records DOCS-02/03/04 as completed, which the milestone audit needs for cross-reference.

Output: Corrected README.md error-handling block and updated 06-03-SUMMARY.md frontmatter.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source of truth for correct property names
@packages/sdk/src/errors/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README error-handling property names</name>
  <files>README.md</files>
  <action>
In README.md, locate the error-handling code block (around lines 166-176). Make exactly two changes:

1. Line 171: Change `err.retryAfter` to `err.retryAfterMs`
   - Before: `console.error(\`Rate limited. Retry after ${err.retryAfter}s\`);`
   - After:  `console.error(\`Rate limited. Retry after ${err.retryAfterMs}s\`);`

2. Line 173: Change `err.status` to `err.statusCode`
   - Before: `console.error(\`API error ${err.status}: ${err.message}\`);`
   - After:  `console.error(\`API error ${err.statusCode}: ${err.message}\`);`

Verify the correct property names against packages/sdk/src/errors/index.ts before editing:
- RateLimitError has `retryAfterMs?: number` (line ~79)
- APIError has `statusCode: number` (line ~92)

Do NOT change any other README sections. The quickstart, API Overview table, platform adapters, services, and examples sections are all correct.
  </action>
  <verify>
Run: `grep -n 'retryAfter\|err\.status' README.md`
Expected output should show `retryAfterMs` and `statusCode` — no occurrences of bare `retryAfter` or `err.status`.

Run: `grep 'me()\|discovery.*suggested' README.md`
Expected output should show `profile.me()` and `discovery.suggested()` in the API Overview table — confirming SC-2 coverage.
  </verify>
  <done>
README error-handling block uses `err.retryAfterMs` and `err.statusCode`, matching the actual SDK error class properties. No other README sections modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add requirements-completed to 06-03-SUMMARY.md frontmatter</name>
  <files>.planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md</files>
  <action>
In .planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md, add a `requirements-completed` field to the YAML frontmatter block (between the `---` delimiters).

Add this line after the `tags:` line:
```yaml
requirements-completed: [DOCS-02, DOCS-03, DOCS-04]
```

IMPORTANT: Do NOT include DOCS-01 — that requirement belongs to Phase 7, not Phase 6. The 06-03-SUMMARY.md is a historical record of what Phase 6 completed.

The resulting frontmatter should look like:
```yaml
---
phase: 06-adapters-docs-and-release
plan: 03
subsystem: docs-and-release
tags: [readme, examples, cloudflare-workers, nextjs, x402, publish-pipeline, attw]
requirements-completed: [DOCS-02, DOCS-03, DOCS-04]
```
  </action>
  <verify>
Run: `grep 'requirements-completed' .planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md`
Expected output: `requirements-completed: [DOCS-02, DOCS-03, DOCS-04]`
Confirm DOCS-01 is NOT present in that line.
  </verify>
  <done>
06-03-SUMMARY.md frontmatter includes `requirements-completed: [DOCS-02, DOCS-03, DOCS-04]` — enabling the milestone audit to cross-reference these requirements as fully satisfied.
  </done>
</task>

</tasks>

<verification>
1. `grep -n 'retryAfterMs' README.md` — shows the corrected RateLimitError property
2. `grep -n 'statusCode' README.md` — shows the corrected APIError property
3. `grep -n 'retryAfter[^M]' README.md` — returns NO matches (old wrong name gone)
4. `grep -n 'err\.status[^C]' README.md` — returns NO matches (old wrong name gone)
5. `grep 'me()\|discovery.*suggested' README.md` — shows profile.me() and discovery.suggested() in API Overview table (SC-2)
6. `grep 'requirements-completed' .planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md` — shows [DOCS-02, DOCS-03, DOCS-04]
7. `grep 'DOCS-01' .planning/phases/06-adapters-docs-and-release/06-03-SUMMARY.md` — returns NO matches (DOCS-01 not backfilled)
</verification>

<success_criteria>
1. README error-handling code block compiles without TypeScript errors — `err.retryAfterMs` and `err.statusCode` match the actual SDK API
2. 06-03-SUMMARY.md frontmatter includes `requirements-completed: [DOCS-02, DOCS-03, DOCS-04]`
3. No other files modified — scope is surgical
</success_criteria>

<output>
After completion, create `.planning/phases/07-readme-and-documentation-fixes/07-01-SUMMARY.md`
</output>
