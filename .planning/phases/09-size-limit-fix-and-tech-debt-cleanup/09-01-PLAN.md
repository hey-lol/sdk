---
phase: 09-size-limit-fix-and-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .size-limit.json
  - packages/sdk/src/auth/index.ts
  - packages/sdk/src/index.ts
  - README.md
autonomous: true
requirements: [INFRA-06]
gap_closure: true

must_haves:
  truths:
    - "`pnpm turbo size-check` exits 0 — no stale dist file references"
    - "`encodeCompactU16` is not importable from `@heylol/sdk` or `@heylol/sdk`'s auth barrel"
    - "`PAYMENT_HEADERS` is not in the named-export list of `packages/sdk/src/index.ts`"
    - "README error-handling example converts `retryAfterMs` to seconds before displaying with `s` suffix"
    - "04-02-SUMMARY.md frontmatter lists POST-01..07 and PROF-01..04 in requirements-completed"
    - "06-02-SUMMARY.md frontmatter lists TYPE-04 in requirements-completed"
  artifacts:
    - path: ".size-limit.json"
      provides: "Bundle size enforcement config referencing only existing dist files"
      contains: "packages/sdk/dist/index.mjs"
    - path: "packages/sdk/src/auth/index.ts"
      provides: "Auth barrel without encodeCompactU16 re-export"
    - path: "packages/sdk/src/index.ts"
      provides: "Public API barrel without PAYMENT_HEADERS"
    - path: "README.md"
      provides: "Correct retryAfterMs display with division by 1000"
      contains: "err.retryAfterMs / 1000"
  key_links:
    - from: ".size-limit.json"
      to: "packages/sdk/dist/index.mjs"
      via: "path field in size-limit config"
      pattern: "packages/sdk/dist/index\\.mjs"
    - from: "packages/sdk/src/index.ts"
      to: "packages/sdk/src/auth/index.ts"
      via: "named re-export block"
      pattern: "from './auth/index\\.js'"
---

<objective>
Fix stale `.size-limit.json` entry, remove leaked internal exports from public API, and correct README milliseconds label.

Purpose: CI size-check is currently broken because `.size-limit.json` references the deleted `services.mjs` dist file. Two internal symbols (`encodeCompactU16`, `PAYMENT_HEADERS`) are unnecessarily exposed in the public API surface. The README error-handling example misleads developers by appending `s` (seconds) to a millisecond value. Additionally, two SUMMARY frontmatter entries need verification (research confirms they are already correct).

Output: All five Phase 9 success criteria satisfied. `pnpm turbo size-check` passes. Public API surface is clean. README displays correct units.
</objective>

<execution_context>
@/Users/rawgroundbeef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rawgroundbeef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-size-limit-fix-and-tech-debt-cleanup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix size-limit config and remove leaked public API exports</name>
  <files>.size-limit.json, packages/sdk/src/auth/index.ts, packages/sdk/src/index.ts</files>
  <action>
Three surgical edits:

1. **`.size-limit.json`** — Remove the stale `services.mjs` entry. The file currently has two entries; remove the second object entirely (the one with `"path": "packages/sdk/dist/services.mjs"`). The result should be a JSON array with a single entry for `packages/sdk/dist/index.mjs`.

2. **`packages/sdk/src/auth/index.ts`** — Remove `encodeCompactU16` from the named export on line 3. Change:
   ```typescript
   export { buildDummyTransaction, encodeCompactU16 } from './solana.js';
   ```
   To:
   ```typescript
   export { buildDummyTransaction } from './solana.js';
   ```
   No other source file imports `encodeCompactU16` from the auth barrel (confirmed via grep — only `solana.ts` uses it internally via its own local reference).

3. **`packages/sdk/src/index.ts`** — Remove `PAYMENT_HEADERS` from the named-export block on line 10. Change:
   ```typescript
   export {
     buildDummyTransaction,
     buildPaymentHeader,
     getPaymentVersion,
     loadKeypair,
     PAYMENT_HEADERS,
     parsePaymentRequirements,
   } from './auth/index.js';
   ```
   To:
   ```typescript
   export {
     buildDummyTransaction,
     buildPaymentHeader,
     getPaymentVersion,
     loadKeypair,
     parsePaymentRequirements,
   } from './auth/index.js';
   ```

After edits, rebuild the SDK to regenerate dist: `pnpm turbo build --filter=@heylol/sdk`
  </action>
  <verify>
1. `cat .size-limit.json` shows only the `index.mjs` entry (no `services.mjs`)
2. `grep encodeCompactU16 packages/sdk/src/auth/index.ts` returns nothing
3. `grep PAYMENT_HEADERS packages/sdk/src/index.ts` returns nothing
4. `pnpm turbo build --filter=@heylol/sdk` succeeds
5. `pnpm turbo size-check` exits 0
6. `grep encodeCompactU16 packages/sdk/dist/index.mjs` — should NOT appear as an export (may appear as internal code, but not in export statement)
7. `grep "export.*PAYMENT_HEADERS" packages/sdk/dist/index.mjs` returns nothing
  </verify>
  <done>
`.size-limit.json` references only existing dist files and `pnpm turbo size-check` passes. `encodeCompactU16` is removed from the auth barrel and `PAYMENT_HEADERS` is removed from the public API barrel. Neither symbol appears in `@heylol/sdk`'s named exports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix README retryAfterMs display and verify SUMMARY frontmatter</name>
  <files>README.md</files>
  <action>
Two actions:

1. **`README.md` line 171** — Fix the misleading seconds suffix on the millisecond value. Change:
   ```typescript
   console.error(`Rate limited. Retry after ${err.retryAfterMs}s`);
   ```
   To:
   ```typescript
   console.error(`Rate limited. Retry after ${err.retryAfterMs / 1000}s`);
   ```
   This converts milliseconds to seconds before displaying, so "Rate limited. Retry after 5s" instead of "Rate limited. Retry after 5000s".

2. **Verify SUMMARY frontmatter (no edits expected):**
   - Confirm `04-02-SUMMARY.md` frontmatter contains `requirements-completed` with POST-01 through POST-07 and PROF-01 through PROF-04. Research confirms this is already satisfied.
   - Confirm `06-02-SUMMARY.md` frontmatter contains `requirements-completed` with TYPE-04. Research confirms this is already satisfied.
   - If either is NOT satisfied (unexpected), add the missing entries. But based on research both are already correct.
  </action>
  <verify>
1. `grep "retryAfterMs / 1000" README.md` finds the corrected line
2. `grep -c "retryAfterMs}s" README.md` returns 0 (old pattern gone)
3. `grep -A 15 "requirements-completed" .planning/phases/04-api-wrappers/04-02-SUMMARY.md` shows POST-01..07 and PROF-01..04
4. `grep -A 3 "requirements-completed" .planning/phases/06-adapters-docs-and-release/06-02-SUMMARY.md` shows TYPE-04
  </verify>
  <done>
README error-handling example displays `retryAfterMs / 1000` with `s` suffix (correct seconds). Both SUMMARY frontmatter files have the required `requirements-completed` entries.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full CI validation — build, lint, typecheck, test, size-check</name>
  <files></files>
  <action>
Run the full CI pipeline to confirm no regressions from the edits:

```bash
pnpm turbo lint typecheck test size-check
```

This validates:
- Lint passes (Biome, ESLint) — no import errors from removed exports
- Type checking passes — no TypeScript errors from removed symbols
- Tests pass — no test imports the removed symbols
- Size-check passes — `.size-limit.json` references only existing files

If any test imports `encodeCompactU16` or `PAYMENT_HEADERS` from the public API (unlikely but possible), update those tests to import directly from the internal module path or remove the import.
  </action>
  <verify>
`pnpm turbo lint typecheck test size-check` exits 0 with all tasks successful.
  </verify>
  <done>
Full CI pipeline passes with zero errors. All Phase 9 success criteria are satisfied: size-check passes, SUMMARY frontmatter correct, leaked exports removed, README units correct.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify all five Phase 9 success criteria:

1. **Size-limit passes:** `pnpm turbo size-check` exits 0
2. **04-02-SUMMARY.md frontmatter:** Contains POST-01..07, PROF-01..04 in `requirements-completed`
3. **06-02-SUMMARY.md frontmatter:** Contains TYPE-04 in `requirements-completed`
4. **No leaked exports:** `encodeCompactU16` and `PAYMENT_HEADERS` not exported from `@heylol/sdk`
5. **README correct:** `retryAfterMs / 1000` displayed with `s` suffix, not raw milliseconds
</verification>

<success_criteria>
- `pnpm turbo size-check` exits 0
- `pnpm turbo lint typecheck test` exits 0
- `grep encodeCompactU16 packages/sdk/src/auth/index.ts` returns nothing
- `grep PAYMENT_HEADERS packages/sdk/src/index.ts` returns nothing
- `grep "retryAfterMs / 1000" README.md` finds the corrected line
- Both SUMMARY frontmatter files have correct `requirements-completed` entries
</success_criteria>

<output>
After completion, create `.planning/phases/09-size-limit-fix-and-tech-debt-cleanup/09-01-SUMMARY.md`
</output>
